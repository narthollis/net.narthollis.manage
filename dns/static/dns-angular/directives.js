//var dnsService = angular.module('dnsService', []);

/*
dnsService.directive('dnsRecord', function() {
    var dnsRecordDefinition = {
        //'controller': ZoneRecordCtrl,
        'link': function(scope, iElement, iAttrs, controller) {
            console.log(this);
            console.log(scope, iElement, iAttrs, controller);
        }
    };
    return dnsRecordDefinition;
});
*/

